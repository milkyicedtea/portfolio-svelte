# Optional: GitHub personal access token for higher API rate limits (5000/hr vs 60/hr unauthenticated)
# Generate one at https://github.com/settings/tokens (no scopes needed for public repos)
GITHUB_TOKEN=
